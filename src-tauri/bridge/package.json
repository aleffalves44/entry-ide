{
  "name": "entry-bridge-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime deps for entry-claude-bridge.mjs, shipped inside the .app bundle. Versions must match the root package.json — enforced by tauri-bundle-resources.test.ts.",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.2.132",
    "zod": "^4.0.0"
  }
}
